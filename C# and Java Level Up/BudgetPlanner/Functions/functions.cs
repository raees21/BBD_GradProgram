using BudgetPlanner.Database;
using BudgetPlanner.Models;

namespace BudgetPlanner.Functions
{
    public class Class
    {
        /*
        public void getData(DBManager dBManager, int month, int catID, int userID)
        {
            dBManager.
        }

        public int sumTransaction(int month,int catID,DBManager dBManager,int user) {
            List<Transactions> AllTransactions = new List<Transactions>();

            foreach (Transactions transactions1 in dBManager.Transactions)
            {
              AllTransactions.Add(transactions1);
            }

            ViewBag.Users = new SelectList(_dBManager.Users, nameof(Users.Token), nameof(Users.UserName)); ; //For Dropdown
            ViewBag.UserTransactions = AllTransactions.Where(x => x.Token == user && x).ToList();

        }

        */
    }
}
